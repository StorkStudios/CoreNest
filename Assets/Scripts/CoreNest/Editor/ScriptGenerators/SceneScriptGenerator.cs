using System.IO;
using System.Linq;
using System.Text;
using System.Text.RegularExpressions;
using UnityEditor;
using UnityEditorInternal;
using UnityEngine;

[InitializeOnLoad]
public class SceneScriptGenerator
{
    public const string enumName = "Scene";
    public const string extension = ".cs";
    public const string targetFolderPath = "Scripts/GameControl";

    private static string currentFileContent = "";
    private static string pathToTargetFolder = "";

    private static string FilePath => Path.Combine(pathToTargetFolder, enumName + extension);

    [MenuItem("Assets/Create/GameManagement/ScenesFile", priority = -20)]
    private static void CreateAndUpdateScenesFile()
    {
        if (TryGetPathToTargetFolder() && !string.IsNullOrEmpty(pathToTargetFolder) && File.Exists(FilePath))
        {
            Debug.LogWarning("Scenes file already exists");
            Update();
            return;
        }

        Directory.CreateDirectory(Path.Combine(Application.dataPath, targetFolderPath));
        File.Create(Path.Combine(Application.dataPath, targetFolderPath, enumName + extension));
        Update();
    }

    static SceneScriptGenerator()
    {
        //initialization of static fields in update because they are reset on domain reload and editor update callback is somewhat not
        //there can be a situation where update is called and fields have default value
        EditorBuildSettings.sceneListChanged += Update;
    }

    private static void Update()
    {
        if (!TryGetPathToTargetFolder() || string.IsNullOrEmpty(pathToTargetFolder))
        {
            return;
        }

        if (string.IsNullOrEmpty(currentFileContent))
        {
            TryGetCurrentFileContent();
        }

        string newFileContent = GetNewFileContent();
        if (newFileContent != currentFileContent)
        {
            currentFileContent = newFileContent;
            File.WriteAllText(FilePath, currentFileContent);

            //force unity to reimport this script so it recompiles it and doesn't throw warnings that "the file has been modified and unity doesn't know when"
            AssetDatabase.ImportAsset(Path.Combine("Assets", targetFolderPath, enumName + extension));
        }
    }

    private static bool TryGetPathToTargetFolder()
    {
        pathToTargetFolder = Directory.GetDirectories(Application.dataPath, targetFolderPath).FirstOrDefault();
        return !string.IsNullOrEmpty(pathToTargetFolder);
    }

    private static bool TryGetCurrentFileContent()
    {
        string filePath = FilePath;
        if (File.Exists(filePath))
        {
            currentFileContent = File.ReadAllText(filePath);
            return true;
        }
        return false;
    }

    private static string GetNewFileContent()
    {
        EditorBuildSettingsScene[] scenes = EditorBuildSettings.scenes;

        StringBuilder stringBuilder = new StringBuilder();
        stringBuilder.AppendLine($"//Script generated by {nameof(SceneScriptGenerator)}.cs");
        stringBuilder.AppendLine($"public enum {enumName}");
        stringBuilder.AppendLine("{");

        int i = 0;
        foreach (EditorBuildSettingsScene scene in scenes)
        {
            string sceneName = Path.GetFileNameWithoutExtension(scene.path);
            string fieldName = ClearSpecialCharacters(sceneName);
            stringBuilder.AppendLine($"\t{fieldName} = {i},");
            i++;
        }

        stringBuilder.AppendLine("}");

        stringBuilder.AppendLine("");

        stringBuilder.AppendLine($"public static class {enumName}Extensions");
        stringBuilder.AppendLine("{");
        stringBuilder.AppendLine($"\tpublic static int GetBuildIndex(this {enumName} value)");
        stringBuilder.AppendLine("\t{");
        stringBuilder.AppendLine("\t\treturn (int)value;");
        stringBuilder.AppendLine("\t}");
        stringBuilder.AppendLine("}");

        return stringBuilder.ToString();
    }

    private static string ClearSpecialCharacters(string str)
    {
        string pattern = "[^a-zA-Z0-9]";
        string replacement = "_";
        return Regex.Replace(str, pattern, replacement);
    }
}
